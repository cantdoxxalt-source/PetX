[build]
builder = "NIXPACKS"
buildCommand = "rm -f package-lock.json && npm install && npm run build && cd backend && rm -f package-lock.json && npm install && npm run db:generate && npm run build"

[deploy]
startCommand = "cd backend && npm run db:migrate && node ../backend/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
